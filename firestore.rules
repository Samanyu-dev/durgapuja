rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users to read and write to all app collections
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
